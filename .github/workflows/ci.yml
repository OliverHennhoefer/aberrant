name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

env:
  UV_VERSION: "0.9.9"

jobs:
  minimal-install-smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Install uv
        uses: astral-sh/setup-uv@37802adc94f370d6bfd71619e3f0bf239e1f3b78 # v7
        with:
          version: ${{ env.UV_VERSION }}

      - name: Install Python
        run: uv python install 3.11

      - name: Install base dependencies only
        run: uv sync --locked

      - name: Base install smoke
        run: |
          uv run --locked python - <<'PY'
          import aberrant
          from aberrant.base import Pipeline
          from aberrant.model import NullModel, QuantileThreshold, RandomModel, ThresholdModel
          from aberrant.model.iforest import OnlineIsolationForest
          from aberrant.transform.preprocessing import StandardScaler

          assert isinstance(aberrant.__version__, str)
          assert NullModel().score_one({"x": 1.0}) == 0.0
          assert 0.0 <= RandomModel().score_one({"x": 1.0}) <= 1.0
          assert ThresholdModel(ceiling=1.0).score_one({"x": 2.0}) == 1.0
          q = QuantileThreshold()
          q.learn_one({"score": 0.1})
          _ = q.score_one({"score": 0.1})
          p = StandardScaler() | OnlineIsolationForest()
          assert isinstance(p, Pipeline)
          PY

  lint-type-test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        python-version: ["3.10", "3.11", "3.12"]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Install uv
        uses: astral-sh/setup-uv@37802adc94f370d6bfd71619e3f0bf239e1f3b78 # v7
        with:
          version: ${{ env.UV_VERSION }}

      - name: Install Python
        run: uv python install ${{ matrix.python-version }}

      - name: Install dependencies
        run: uv sync --locked --extra dev

      - name: Ruff
        run: uv run --locked python -m ruff check .

      - name: Mypy
        run: uv run --locked python -m mypy aberrant

      - name: Unit tests
        run: uv run --locked python -m pytest tests/models tests/drift tests/transformers -q

  extras-smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Install uv
        uses: astral-sh/setup-uv@37802adc94f370d6bfd71619e3f0bf239e1f3b78 # v7
        with:
          version: ${{ env.UV_VERSION }}

      - name: Install Python
        run: uv python install 3.11

      - name: Install extras
        run: uv sync --locked --extra dev --extra faiss

      - name: Extras import smoke
        run: |
          uv run --locked python - <<'PY'
          from aberrant.model.deep import Autoencoder, OnlineAutoencoderEnsemble
          from aberrant.utils.similar.faiss_engine import FaissSimilaritySearchEngine

          assert Autoencoder is not None
          assert OnlineAutoencoderEnsemble is not None
          engine = FaissSimilaritySearchEngine(window_size=8, warm_up=2)
          engine.append({"x": 0.0, "y": 0.0})
          engine.append({"x": 1.0, "y": 1.0})
          assert isinstance(engine.search({"x": 0.5, "y": 0.5}, n_neighbors=1), float)
          PY

      - name: FAISS model smoke tests
        run: uv run --locked python -m pytest tests/models/test_distance_knn.py -q

  integration-smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Install uv
        uses: astral-sh/setup-uv@37802adc94f370d6bfd71619e3f0bf239e1f3b78 # v7
        with:
          version: ${{ env.UV_VERSION }}

      - name: Install Python
        run: uv python install 3.11

      - name: Install dependencies
        run: uv sync --locked --extra dev

      - name: Integration smoke
        run: uv run --locked python -m pytest tests/test_public_imports_base.py tests/test_public_imports_extras.py tests/integration -q

  coverage:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Install uv
        uses: astral-sh/setup-uv@37802adc94f370d6bfd71619e3f0bf239e1f3b78 # v7
        with:
          version: ${{ env.UV_VERSION }}

      - name: Install Python
        run: uv python install 3.11

      - name: Install dependencies
        run: uv sync --locked --extra dev

      - name: Coverage
        run: uv run --locked python -m pytest tests/models tests/drift tests/transformers --cov=aberrant --cov-report=xml --cov-report=term-missing -q

      - name: Upload coverage artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: coverage-xml
          path: coverage.xml

  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Install uv
        uses: astral-sh/setup-uv@37802adc94f370d6bfd71619e3f0bf239e1f3b78 # v7
        with:
          version: ${{ env.UV_VERSION }}

      - name: Install Python
        run: uv python install 3.11

      - name: Build sdist and wheel
        run: uv build
